extends layout

block content
  header.center
    h1= title
  main.main
    p= prompt

  .buttons.top-margin
    a.button.green#start.button(onclick=`assignUrl('${surveyUrl}')`) Continue

  script.
    function assignUrl(url) {
      window.location.assign(url)
    }
